# BeeMovieC
The Entire Bee Movie displayed in terminal using C

Compiled using `gcc` and msys
WINDOWS ONLY
Linux support soon

use `make` or `mingw32-make` to compile
Exe file is in `dist/beemovie.exe`
